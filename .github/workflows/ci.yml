name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
 
  BUILD_TYPE: TestBuild

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Configure CMake
      run: ./SConstruct


          